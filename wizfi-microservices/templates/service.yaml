apiVersion: v1
kind: Service
metadata:
  name: {{ include "wizfi-microservices.fullname" . }}
  labels:
    {{- include "wizfi-microservices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wizfi-microservices.selectorLabels" . | nindent 4 }}
